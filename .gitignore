# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Client-specific env files (keep keys out of git)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env.e2e
test-results/
coverage/
*.docx

# Vercel CLI: link del proyecto + VERCEL_OIDC_TOKEN en .env.local
.vercel
.env*
# Reafirmado DESPUES de `.env*`: la regla de arriba lo volvia a ignorar y
# el .env.example es justamente el que SI tiene que estar en el repo.
!.env.example

# Claude Code: las skills se comparten, la config local no
.claude/settings.local.json

# Temporales del CLI de Supabase (los deja el deploy de functions)
supabase/.temp/

# DICO-QA-Lite: runtime local, migraciones copiadas y evidencia generada.
platform/qa-lite/supabase/migrations/
platform/qa-lite/supabase/seed.sql
platform/qa-lite/supabase/.temp/
platform/qa-lite/supabase/.branches/
.qa-lite/
